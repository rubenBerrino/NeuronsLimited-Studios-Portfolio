import RevealWrapper from "./RevealWrapper";

export default function Hero() {
  return (
    <RevealWrapper>
      <section className="hero">
        <h1>Developing games, one neuron at a time.</h1>
        <p>
          We are a small indie team dedicated to crafting quirky, silly, and
          genuinely fun gaming experiences.
        </p>
      </section>
    </RevealWrapper>
  );
}